// FIX: The reference to "vite/client" was removed to resolve a type definition error.
// The application has been updated to use `process.env.API_KEY` as per the guidelines,
// so Vite-specific environment variable typing is no longer required for the API key.
